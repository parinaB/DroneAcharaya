# DroneAcharaya backend — target: Python 3.11
fastapi
uvicorn[standard]
pydantic
pydantic-settings
numpy
pandas
scikit-learn
xgboost

# CPU-only build: this is a serving API, not a training environment (see
# ml/CLAUDE.md's Commands section for GPU training). Unpinned `torch` pulls
# PyPI's default CUDA-bundled wheel plus its full nvidia-*/cuda-toolkit
# dependency chain (several GB), which has been enough to exhaust CI runner
# disk space outright. The CPU wheel is ~200MB and installs cleanly.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu

shap
python-multipart

# persistence (Step 9 bridge/replay -- Supabase Postgres in prod, sqlite
# locally by default so `uvicorn app.main:app --reload` works with zero
# setup; see backend/app/core/config.py's database_url default)
sqlalchemy
alembic
psycopg2-binary

# dev / test
pytest
httpx
pytest-cov
